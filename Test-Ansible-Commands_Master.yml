---
- hosts: all
  vars:
    hostname: shasha
  tasks:
    - set_fact:
        catalogNameM: "{{catalog_url.split('/')[-1]}}"
    - import_playbook: ActivateCatalog.yml
      vars:
        catalogName: "{{catalogNameM}}"